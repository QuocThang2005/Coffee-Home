{
  "name": "coffee-home",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Coffee Home - Web dat nuoc va dat ban quan ca phe (Vite MPA + FastAPI backend)",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/postbuild.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
